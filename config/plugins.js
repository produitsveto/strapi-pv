/**
 * Aucun plugin à configurer ici pour l'instant.
 *
 * Le plugin local « Traduction » (bouton de relance à la demande dans le Content
 * Manager) y était déclaré ; il a été retiré le 03/09/2026 au profit d'une passe
 * nocturne entièrement automatique — plus rien à cliquer, donc plus de plugin.
 *
 * Le fichier est conservé : la configuration de production (`config/env/production`)
 * s'appuie dessus pour le stockage des médias sur R2.
 */
module.exports = () => ({});
